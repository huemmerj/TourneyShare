package components

templ FooterItem (name string, href string) {
	<div class="flex flex-col text-center flex-1 items-center justify-center">

		<button hx-get={href} hx-push-url="true" hx-target="#main">{name}</button>
	</div>

}
